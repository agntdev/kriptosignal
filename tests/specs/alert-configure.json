[
  {
    "name": "Configure alerts shows empty state",
    "steps": [
      {
        "send": { "callback": "alert:configure" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "No alerts set yet.\n\nTap 🔔 Set alert to create one."
            }
          }
        ]
      }
    ]
  }
]
